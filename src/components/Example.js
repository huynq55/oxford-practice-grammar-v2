import React, { Fragment } from 'react';

const Example = (props) => {
  return (
    <Fragment>
      <span style={{ color: 'blue' }}>{'\u25b6'}</span>
    </Fragment>
  );
};

export default Example;
